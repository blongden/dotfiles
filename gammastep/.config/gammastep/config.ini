; Optional blue-light filter (off by default — you didn't run redshift before).
; To enable: uncomment `gammastep &` in ~/.config/sway/autostart.sh
[general]
temp-day=6500
temp-night=4200
fade=1
adjustment-method=wayland
location-provider=manual

[manual]
; London-ish; adjust to taste
lat=51.5
lon=-0.1
