[user]
        name = Ben Longden
        email = ben@longden.me
[github]
        user = blongden
[core]
        editor = /usr/local/bin/nvim
        pager = diff-so-fancy | less --tabs=4 -RFX
[alias]
        lg = log --graph --pretty=format:'%Cred%h%Creset %C(blue)%G?%Creset %C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit  --date=relative
[push]
        default = simple
        
[hub]
        protocol = https
        
[credential]
[rerere]
        enabled = true
[filter "lfs"]
        clean = git-lfs clean %f
        smudge = git-lfs smudge %f
        required = true
